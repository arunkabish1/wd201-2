<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Sign Up</title>
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link rel="icon" type="image/x-icon" href="/images/icons8-to-do-list-16.ico">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <title><%= title %></title>
    <link href="./css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-200 p-8">
    <%- include("header.ejs") %> 
   <br>
    <form action="/session" method="post" class="bg-white p-4 shadow-md rounded-md w-1/2 mx-auto">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <h2 class="py-2 text-center text-lg mb-2"><b>Login to your account </b></h2>
        <h4> <p class="py-2 text-lg border-b-2 mb-2">Don't have an account? <a href="/signup" class="text-gray-600 font-bold hover:text-green-500 ">Create one</a></h4>
        
        <!-- <div class="mb-4">
            <label for="fname" class="block text-gray-600">First Name:</label>
            <input type="text" id="firstName" name="firstName" required class="w-full border p-2 rounded-md">
        </div>

        <div class="mb-4">
            <label for="lname" class="block text-gray-600">Last Name:</label>
            <input type="text" id="lname" name="lname" required class="w-full border p-2 rounded-md">
        </div> -->

        <div class="mb-4">
            <label for="email" class="block text-gray-600">Email:</label>
            <input type="email" placeholder="email" id="email" name="email" required class="w-full border p-2 rounded-md">
        </div>

        <div class="mb-6">
            <label for="password" class="block text-gray-600">Password:</label>
            <input type="password" placeholder="password" id="password" name="password" required class="w-full border p-2 rounded-md">
        </div>

        <div class="text-center">
            <button type="submit" id="Login" name="submit" hover:bg-green-500 class="bg-blue-500 text-white py-2 px-4 rounded-md cursor-pointer">login</button>
        </div>
        <br><hr><br>
        <div class="text-center" >
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class="rounded-full border-2 p-2 bg-gray-600 text-center text-white"> <%= messages.error[i] %></p>
                <% } %>
              <% } %>
        </div>
    </form><br>
    
      <footer class="text-center p-5" >
        <p><%- include ('footer.ejs') %></p>
        
      </footer>
    
    
</body>
</html>
